node_modules
dist
.vite
test-results
playwright-report
blob-report
playwright/.cache
.DS_Store
*.log
.env
.env.*
!.env.example
# Host keck SQLite fallback (not the product store)
/data
# Rust workspace (commit Cargo.lock for the hub binary)
/target
# Nested wiki git (sidecar autoinit on first snapshot). Not the product remote.
/wiki
/wiki/
